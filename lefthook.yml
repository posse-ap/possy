pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: npm run lint {staged_files}
    format:
      glob: "*.{js,ts,jsx,tsx,json,css,md,yml}"
      run: npm run format {staged_files}
